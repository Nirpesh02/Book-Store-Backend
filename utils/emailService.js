export const sendRefundEmail = async (userEmail, userName, bookTitle, status, comment) => {
  // Nodemailer removed as requested
  console.log(`Refund email logic removed. (Refund: ${status} for ${bookTitle} to ${userEmail})`);
};
